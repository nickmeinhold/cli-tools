# Plaintext template — do NOT commit a populated copy.
# Populate via ./set-credentials.sh, which writes this file directly and
# sops-encrypts it in place — plaintext never printed, logged, or left on disk.
client_number: ""
password: ""
